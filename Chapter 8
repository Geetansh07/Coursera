#Assignment 8.4
fname = input("Enter file name: ")
fh = open(fname)
lst = list()
for line in fh:
    line.rstrip()
    line = line.split()
    for i in line:
        if i in lst: continue
        lst.append(i)
print(sorted(lst))
